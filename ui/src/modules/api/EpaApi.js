'use strict';

module.exports = function(ngModule) {
    
    ngModule.service('EpaApi', function($http, $q) {
        
        
        this.getAirQuality = function() {
            
        };
        
    });
    
};
