/**
 * First test... tests that karma, jasmine, and the rest of the toolchain are
 * setup properly and working.
 */
describe('Test Scaffolding', function() {

    it('can execute a single unit test with no dependencies', function() {
        expect(true).toBe(true);
    });

});

