'use strict';

//var airQualityService = require('../../services/airQualityService')();
//var config = require('../../config');

var should = require('should');

describe('GeocoderService', function() {
    beforeEach(function(done) {
        // No shared setup presently

        done();
    });

    it('should retrieve location of valid zip code', function(done) {
        ''.should.equal('Not Implemented');

        done();
    });

    it('should fail when querying invalid zip code', function(done) {
        ''.should.equal('Not Implemented');

        done();
    });
});
